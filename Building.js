// Declaration
class Building {
    constructor(cost, cat_limit) {
      this.cost = cost;
      this.cat_limit = cat_limit;
    }
  
    // Getter - cost
    get cost() {
      return this.cost();
    }
  
    // Getter - cat limit
    get cat_limit() {
      return this.cat_limit();
    }
  
  }